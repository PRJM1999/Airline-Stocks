# server {
#     listen 80;
#     server_name airlinestock.co.uk; # Replace with your domain name

#     location ~ /.well-known/acme-challenge/ {
#         root /var/www/certbot/;
#     }

#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# server {
#     listen 443 ssl;
#     server_name airlinestock.co.uk; # Replace with your domain name

#     ssl_certificate /etc/letsencrypt/live/airlinestock.co.uk/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/airlinestock.co.uk/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

#     location / {
#         root   /usr/share/nginx/html;
#         index  index.html index.htm;
#         try_files $uri /index.html;                 
#     }

#     error_page 500 502 503 504 /50x.html;
#     location = /50x.html {
#         root /usr/share/nginx/html;
#     }

#     location /backend {
#         proxy_pass http://localhost:3000;
#     }
# }

server {
    listen       80;
    server_name  airlinestock.co.uk;

    # Certbot allow all traffic to the .well-known directory
    location ~ /.well-known/acme-challenge/ {
        allow all;
        root /var/www/certbot;
        try_files $uri =404;  # This ensures that Nginx attempts to serve the file if it exists, or returns a 404 error otherwise.
    }

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri /index.html;                 
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    location /backend {
        proxy_pass http://localhost:3000;
    }

    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    #     allow all;
    #     try_files $uri =404;  # This ensures that Nginx attempts to serve the file if it exists, or returns a 404 error otherwise.
    # }
}